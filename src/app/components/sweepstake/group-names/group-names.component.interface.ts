import {IParticipant} from '../../../interfaces/party.interface';

export interface IGroupNames {
  appGroupNamesParticipants: IParticipant;
  appGroupNamesIsComplete: boolean;
  appGroupNamesNumberParticipants: number;
}
